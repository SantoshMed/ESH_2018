import { NewTabDirective } from './new-tab.directive';

describe('NewTabDirective', () => {
  it('should create an instance', () => {
    const directive = new NewTabDirective();
    expect(directive).toBeTruthy();
  });
});
